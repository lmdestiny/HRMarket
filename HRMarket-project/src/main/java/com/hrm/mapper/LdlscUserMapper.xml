<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hrm.dao.LdlscUserMapper" >
<sql id="bsql">
	user_id as userId,
	user_name as userName,
	userloginname,
	userpassword,
	userindex
</sql>
 	<select id="find" resultType="com.hrm.vo.LdlscUser">
 		select <include refid="bsql"/> from ldlsc_user where 
 		userloginname = #{userloginname} and userpassword =#{userpassword}
 	</select>
</mapper>